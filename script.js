document.getElementById('image_7').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_2').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_3').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_10').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_5').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_6').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_8').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});

document.getElementById('image_9').addEventListener('click', function() { 
    this.classList.add('rotate'); 
    
    // Retirer la classe après l'animation pour pouvoir la rejouer 
    setTimeout(() => { 
        this.classList.remove('rotate');
    }, 1000); // La durée doit correspondre à celle de l'animation CSS
});